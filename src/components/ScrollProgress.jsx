/**
 * ScrollProgress Component
 * ========================
 * Fixed progress bar at the top of the viewport.
 * Width is controlled by the scroll handler in page.js.
 */
export default function ScrollProgress() {
  return <div id="scroll-progress"></div>;
}
